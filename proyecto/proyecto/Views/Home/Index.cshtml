@model proyecto.Models.libromodel
@{
    ViewBag.Title = "Home Page";
 
}
<h4 id="ps"> ULTIMOS LIBROS PUBLICADOS<h4>
<br/>
@foreach (var a in @ViewBag.lista2)
{
    string ruta = "../Content/imagenes/" + @a.portada;
    string titulo = @a.titulo;
    int ide = @a.id;  

    <div class="Roger">
  	    <div class="container_24">
    	    <div class="row">
      	    <div class="wrapper">
        	    <div class="grid_16">
          	    <div class="box">
                  <div class="wrapper">
                            
              	            <img src="@ruta" alt="por si no da" class="img-indent">
                            <h4 class="alt"> @a.titulo</h4>
                            <h4> @a.autor</h4>
                            <br/> 
                    
                    @a.descripcion

                  </div>
                </div>
              </div>
        </div>
        </div>
      </div>
    </div>
} 

<h4 id="ps">ULTIMOS ARTICULOS PUBLICADOS<h4><br/>
@foreach (var item in @ViewBag.lista1)
{
    string imagen1 = "../Content/imagenes/" + @item.portada;
    string titulo = @item.titulo;
    int ide1 = @item.id;  

    <div class="Roger">
  	    <div class="container_24">
    	    <div class="row">
      	    <div class="wrapper">
        	    <div class="grid_16">
          	    <div class="box">
                  <div class="wrapper">
                 
              	    <img src="@imagen1" alt="por si no da" class="img-indent">
                    <h4 class="alt">@item.titulo</h4>
                    <h4>Publicado por : @item.nombre  @item.apellido</h4>
                    <h4> @item.fecha_publicacion</h4><br/>
                    @item.descripcion


                  </div>
                </div>
              </div>
        </div>
        </div>
      </div>
    </div>
}

<h4 id="ps"> ULTIMOS TUTORIALES PUBLICADOS<h4><br/>
@foreach (var item in @ViewBag.lista3)
{
    string ruta = "../Content/imagenes1/" + @item.portada;
    string titulo = @item.titulo;
    int ide1 = @item.id;  

    <div class="Roger">
  	    <div class="container_24">
    	    <div class="row">
      	    <div class="wrapper">
        	    <div class="grid_16">
          	    <div class="box">
                  <div class="wrapper">
                 
              	    <img src="@ruta" alt="por si no da" class="img-indent">
                    <h4 class="alt">@item.titulo</h4>
                    <h4>Publicado por  @item.nombre  @item.apellido</h4>
                    <h4> @item.fecha_publicacion</h4><br/>
                    @item.descripcion

                  </div>
                </div>
              </div>
        </div>
        </div>
      </div>
    </div>
} 

<h4 id="ps"> ULTIMOS CURSOS PUBLICADOS<h4><BR/>
@foreach (var item in @ViewBag.lista4)
{
    string ruta = "../Content/imagenes2/" + @item.portada;
    string titulo = @item.titulo;
    int ide1 = @item.id;  

    <div class="Roger">
  	    <div class="container_24">
    	    <div class="row">
      	    <div class="wrapper">
        	    <div class="grid_16">
          	    <div class="box">
                  <div class="wrapper">
                 
              	    <img src="@ruta" alt="por si no da" class="img-indent">
                    <h4 class="alt">@item.titulo</h4>
                    <h4> @item.fecha_publicacion</h4>
                    <h4>Publicado por :  @item.nombre</h4><br/> 
                    @item.descripcion
                  </div>
                </div>
              </div>
        </div>
        </div>
      </div>
    </div>
} 


