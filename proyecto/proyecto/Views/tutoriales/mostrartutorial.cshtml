@{
    ViewBag.Title = "mostrartutorial";
}

<h4 id="ps">ESTOS TODOS LOS TUTORIALES INGRESADOS EN EL SISTEMA</h4><br/>
<h4 id="ps">DESEA PUBLICAR UN TUTORIAL HAGA CLICK @Html.ActionLink("AQUI", "tutorial", "tutoriales")</h4><br/><br/>
@foreach (var item in @ViewBag.lista1)
{
    string ruta = "/Content/imagenes1/" + @item.portada;
    string titulo = @item.titulo;
    int ide1 = @item.id;  

    <div>
  	    <div class="container_24">
    	    <div class="row">
      	    <div class="wrapper">
        	    <div class="grid_16">
          	    <div class="box">
                  <div class="wrapper">
                 
              	    <img src="@ruta" alt="por si no da" class="img-indent">
                    <h4 class="alt">@Html.ActionLink(titulo,"detalletutorial", new { id=ide1})</h4>
                    <h4>Publicado por  @item.nombre  @item.apellido</h4><br/>
                    <h4> @item.fecha_publicacion</h4><br/>
                    @item.descripcion
                 

                  </div>
                </div>
              </div>
        </div>
        </div>
      </div>
    </div>
} 

